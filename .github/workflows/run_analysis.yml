name: "🚀 Analyze GitHub Projects"

on:
  schedule:
    - cron: '0 */6 * * *'
  workflow_dispatch:

jobs:
  analyze:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
        with:
          fetch-depth: 0   # нужно для push и pull

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run analysis
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: python main.py

      - name: Commit and push reports
        run: |
          git config --global user.email "actions@github.com"
          git config --global user.name "GitHub Actions Bot"

          git add reports/
          if git diff --cached --quiet; then
            echo "🟢 Нет новых файлов для коммита."
          else
            git commit -m "🧠 Update analysis reports [skip ci]"
            echo "🔄 Обновляем локальную ветку перед пушем..."
            git pull --rebase origin main || echo "⚠️ Не удалось выполнить pull (возможно конфликт, продолжаем)"
            git push origin main || echo "⚠️ Не удалось запушить изменения, проверь конфликты"
            echo "✅ Попытка пуша завершена."
          fi
